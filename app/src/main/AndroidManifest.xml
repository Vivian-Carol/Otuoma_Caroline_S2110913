<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Request coarse location permission -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Request fine location permission -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Otuoma_SkyWatch_WeatherApp_S2110913"
        tools:targetApi="31"
        android:hardwareAccelerated="false">
        <activity
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.ManualActivity"
            android:theme="@style/Base.Theme.Otuoma_SkyWatch_WeatherApp_S2110913">
        </activity>
        <activity
            android:name=".views.WeatherLatestObservationsActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Base.Theme.Otuoma_SkyWatch_WeatherApp_S2110913">
            <!-- Intent filters if any -->
        </activity>

        <activity
            android:name=".views.MapDisplayActivity"
            android:theme="@style/Base.Theme.Otuoma_SkyWatch_WeatherApp_S2110913">
            <!-- Intent filters if any -->
        </activity>
        <activity
            android:name=".views.SettingsActivity"
            android:theme="@style/Base.Theme.Otuoma_SkyWatch_WeatherApp_S2110913">
            <!-- Intent filter -->
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBqXaOAd_xmBw0f6GBBW_2WCUqfvohxiLw"/>

    </application>

</manifest>